# Phase 4-I — 真人 UI 走查记录（UAT）

> **状态：🟡 第三轮（整改后**定向复测**）待执行**
>
> **本轮版本：`30c1dc2`**（截至本文成稿；走查当天以前哨打印的 `git rev-parse --short HEAD` 为准）
>
> 第二轮真人走查判定为 **P0×1 + P1×2**，三项均已整改并通过自动化
> （`DEV-70` / `DEV-71` / `DEV-72`）。按复核方口径，**本轮不重新完整走 8 步**，
> 只让**同一真人**定向复测第三轮范围里的那 3 件事（§0）。
>
> ⚠️ **本文件是公开仓库的一部分。** 因此：
> · 走查人只记**代号**（`门店售后人员 UAT-A`），**不写真实姓名、手机号、邮箱**；
> · **不写任何口令**（口令只在本机 `.env` 与当面口头交付）；
> · 工单号可以写（`FW...` 是系统流水号，不含客户个人信息）；
> · 若业务上确需真实签名，签名留在**内部记录**，不写进本文件。

---

## 0. 本轮范围（**先读这一节，避免重复劳动**）

### 0.1 前两轮已经**由真人证明**、本轮**不再重验**的结论

| 已证结论 | 来源 | 处置 |
|---|---|---|
| UAT-A 只能看到 S01 数据 | 首轮 | ✅ 保留，不重验 |
| UAT-B 只能看到 S02 数据 | 首轮 | ✅ 保留，不重验 |
| UAT-HQ 可以看到两店数据 | 首轮 | ✅ 保留，不重验 |
| 工单号搜索可用 | 首轮 | ✅ 保留，不重验 |
| 服务端对象级隔离成立（直链越权 404） | 首轮 | ✅ 保留，不重验 |
| 「受理」完成后状态立即变为「处理中」= 足够反馈 | 第二轮 | ✅ 保留，**不要求**额外"受理成功"提示 |
| 页面上自动生成的「编辑 / 删除」未造成实际操作障碍 | 第二轮 | ✅ 保留，登记 UX backlog（**不阻塞**） |

> ⚠️ 「编辑 / 删除」的**服务端** ACL（两个业务角色对 `serviceTickets`
> 只有 `get`/`list`/`view`，无 `update`/`destroy`）**不得为了"让按钮看起来有用"而放宽**。
> 它是纯 UX 问题，不是权限问题。

### 0.2 第二轮判出的三个问题（**已整改，本轮就是来验它们的**）

| 编号 | 级别 | 第二轮现象 | 根因（已定位） | 整改 |
|---|---|---|---|---|
| **P0** | 阻塞 | 改派时**填了原因**，服务端回 `MISSING_REASON`；界面表现"填了没用" | 客户端载荷**按白名单挑字段**构造，而改派复用了「派工」的构造器（只认 5 个派工字段，**不含 `reason`**）→ `reason` 在出口被静默剔除 | 改派有**自己的**字段集与构造器；客户端按动作各自取参（`DEV-70`） |
| **P1** | 非阻塞 | 「预计上门**时间**」要求选到分钟，但系统不采集签到/到达/GPS/排程 → 那个时分是**伪精度** | 用精度伪装确定性：页面上的"09:37"会被读成"师傅 9 点半到" | 派工与改约 UI 一律改「预计上门**日期**」（日期选择器）；落库统一归一到当天 12:00，且**不显示、不写进事件、不当 SLA 依据**（`DEV-71`） |
| **P1** | 非阻塞 | 详情抽屉"四块内容"平铺，回答不了"当前谁处理 / 哪天上门 / 之前发生了什么" | 信息层级按**数据表**组织，而不是按**售后同事要看的问题**组织 | 抽屉重构为四层（摘要 / 客户与问题 / 当前服务 / 处理记录时间线），并统一隐藏内部字段（`DEV-72` 同批） |

> 📌 本轮**只复测这 3 项**（§2）。其余步骤与结论沿用前两轮 ——
> 因为它们要么已由真人证明，要么不在本轮范围内。

### 0.3 本轮要证伪的命题

> 售后同事能否**在没有操作指引的情况下**，把"改派 + 改预约 + 看详情"这三件事
> **一次做对**，并且**看完详情后能用自己的话**回答：
> **当前谁在处理、预计哪天上门、之前发生了什么**。

---

## 0.4 四层验收模型（本轮沿用，后续后台 UI 功能统一口径）

| 层 | 判据 | 由谁保证 | 本轮状态 |
|---|---|---|---|
| **① 代码层** | 模型类 / 逻辑存在 | 自动化 | ✅ |
| **② 结构层** | 真实 `flowModels` 里挂载正确（**顶层 `use`** 命中 + 挂在 `TableActionsColumnModel` 下 + tree path 连通） | `verify-ticket-actions.mjs`（10 项） | ✅ |
| **③ 呈现层** | 浏览器**真实渲染出按钮** / **抽屉真的能打开并取到数据** | 无头浏览器探针 + `uat-preflight.mjs` §3.6 / **§3.7** | ✅ |
| **④ 业务层** | **真人完成完整任务** | **本次复测** | ⬜ |

> **核心口径**：`ActionModel 注册 ≠ 实例存在 ≠ 用户真实可操作`。
> 只做 ①② 会漏掉"库里全对、页面空白"（DEV-69）；
> 只做 ①②③ 会漏掉"按钮能点，但人不知道怎么用"。
> 本轮还补了一条：**③ 才发现得了"客户端自己把请求拼错"**（DEV-72 的 `/api/api/` 404）——
> HTTP 层断言永远是绿的，因为它测的是服务端，不是调用方。

---

## 1. 走查前置条件（执行前逐条勾掉）

| 项 | 要求 | 状态 |
|---|---|---|
| 代码版本 | 工作区干净；`git rev-parse --short HEAD` 与本文版本一致 | ⬜ 现场确认 |
| 环境 | `docker compose ps` 三容器 healthy；`curl -s localhost:8080/api/svc/health` 返回 `status: ok` | ⬜ 现场确认 |
| 前哨 | `node scripts/uat-preflight.mjs` **21/21 全绿**（含 §3.6 动作实例闸门、**§3.7 H3 抽屉渲染闸门**） | ✅ 2026-09-23 |
| 账号 | 三个临时账号已建（`node scripts/uat-accounts.mjs --create`），口令**当面**交给走查人 | ⬜ 现场确认 |
| UAT 工单 | 主走查单门店 S01 一张 `NEW`；反证单门店 S02 一张 | ⬜ 现场确认 |
| 基线复位 | `node scripts/uat-reset-baseline.mjs` 全绿 | ✅ 2026-09-23 |
| 走查人 | **第二轮那一位**（同一真人 —— 他见过整改前的表现，才判得出有没有变好） | ⬜ |
| 主持方式 | **不给操作指引**（见 §4「禁止预提示」） | ⬜ |
| 浏览器 | 名称与版本：____________________ | ⬜ |

### 走查前标准复位（**先跑这一步，再填上面的表**）

```bash
node scripts/uat-reset-baseline.mjs
```

内部顺序**固定**，不可颠倒：

| 阶段 | 内容 |
|---|---|
| ① 全量验证 | `verify-config` → `verify-plugin-load` → `verify-client-logic` → `verify-phase3-h5` → `verify-ticket-actions` → **`verify-reassign-contract`** → `smoke-test`，**脚本间自动冷却**（`verify-phase3-h5` 前 40s，其余 25s） |
| ② 复位洁净基线 | `scripts/sql/uat-sweep-noise.sql`（删脚本噪声）→ `scripts/sql/uat-reset-fixtures.sql`（把 4 张 UAT 单打回 `NEW`） |
| ③ 前哨复核 | 跑 `uat-preflight.mjs`，打印前哨结果 + 现场 `git rev-parse --short HEAD` |

为什么顺序不能反：**`smoke-test` 会真实调用匿名报修接口造工单**，
"先复位再验证"等于白复位（DEV-61 / DEV-63）。

**期望输出（2026-09-23 实测，全部退出码 0）：**

| 脚本 | 项数 |
|---|---|
| `verify-config` | 48 |
| `verify-plugin-load` | 59 |
| `verify-client-logic` | **50** |
| `verify-phase3-h5` | 35 |
| `verify-ticket-actions` | 10 |
| `verify-reassign-contract` | **11**（含 `--reverse` 反向验证 6 项） |
| `smoke-test` | **116 通过 · 1 项显式跳过** |
| **合计** | **329 项通过 · 1 项显式跳过** |

> ⚠️ **那 1 项"跳过"必须如实读**：它是 `smoke-test` 里唯一覆盖 `svc:visits` 的断言，
> 需要"库里至少有一条 Visit"才成立，而洁净基线把 Visit 清成 0（见 DEV-73）。
> **跳过项不计入"通过"，也不代表已验证** ——
> 同主题断言已搬到 `verify-reassign-contract` 的 **A7**（那个脚本自带 Visit 夹具）。
> 看到 `· 跳过 1 项` 是**预期**，不是缺陷；但也**不要**把它当成 117 项全绿。

> 冷却不足时会出现 **429 假红**（限流不是缺陷）。看到红灯里带 429 字样时**不要改业务代码**，
> 先单独复跑该脚本；转绿即编排问题。详见 DEV-63。
> （全绿运行时**不会**再出现"含 N 处限流迹象"的提示 —— 那条提示现在只在红灯时出现。）

### 环境事实（由前哨固定，走查前填写一次）

| 项 | 值 |
|---|---|
| **UAT 执行版本**（走查当天 HEAD） | `________` ← 前哨现场打印 `git rev-parse --short HEAD` 填入 |
| **功能代码基线** | `30c1dc2`（P0/P1×2 整改；如需精确对应见 `docs/DEVIATIONS.md` DEV-70~73） |
| 日期时间 | ________________（开始）／________________（结束） |
| 走查人 | 门店售后人员 **UAT-A**（角色 `store_after_sales`，门店 S01） |
| 主走查工单 | `FW20260922-0059`（id=1039，门店 S01，起始状态 `NEW`） |
| 反证工单（门店 B） | `FW20260922-0060`（id=1040，门店 S02，账号 UAT-B） |
| 起始基线 | 库内共 **4** 张工单 · Visit **0** 条 · 事件 **0** 条 · `handler_user_id` 为空 |
| ⚠️ 同店干扰单 | 门店 S01 当刻还有 **1 张**其他 `NEW` 工单 → **开场必须报出工单号**，否则真人可能处理错单 |

---

## 2. 本轮唯一任务：定向复测 3 项

> 记录口径：**通过 = 真人独立完成且界面反馈正确**。
> 若需开发人员指点才完成，标 🟡 并写明"卡在哪一步、提示了什么"——
> **🟡 本身就是重要的验收结果**，不要为了好看而改写为通过。

| # | 要复测的事 | 怎么做（给主持人的任务编排） | 预期结果 | 结果 | 证据 / 备注 |
|---|---|---|---|---|---|
| 0 | **前置**：把主走查单处理到"已派工" | "先受理这张单，派给**王师傅**（服务方式选**厂家**，厂家名称填 `UAT测试厂家`）" | 状态变「处理中」；出现 **Visit #1**（已派工） | ⬜ | 第二轮这两步已通过，**本轮属"顺带重做"**（改派必须有一条在案的派工）。只有出问题时才记为红灯 |
| 1 | **P0 改派 + 原因** | "把它**改派给李师傅**，原因随便写一句" | 提交**成功**（不再是 `MISSING_REASON`）；Visit #1 仍在且为「已作废」，Visit #2 为「已派工」 | ⬜ | **改派原因填入值**：____________________<br>是否仍报"缺少原因"：⬜ 是（=P0 未修好） / ⬜ 否<br>Visit 条数：______（预期 **2**） |
| 2 | **P1 预计上门日期** | "把这张单的**预计上门日期**改到后天" | 日期控件**只到天**（没有时分）；提交成功；**业务含义清楚**（"约的是哪一天"） | ⬜ | 新的预计上门日期：____________________<br>界面是否**出现了钟点**（如 12:00）：⬜ 出现了（=未整改） / ⬜ 没出现<br>Visit 条数：______（预期**仍为 2**，改约不新增） |
| 3 | **P1 详情能回答三个问题** | "打开这张单的**详情**看一眼" | 看完后能**自己说清**：当前谁在处理 / 预计哪天上门 / 之前发生了什么 | ⬜ | 逐条见 §5「本轮三个必答问题」 |
| 4 | **反证不回归** | 切 **UAT-B**（门店 B） | **不能**看到、也**不能**通过直链打开这张 S01 工单 | ⬜ | 直链打开结果：____________________ |

> ⚠️ **第 0 步为什么要重做**：改派的前置是"这张单已有在案的派工"。
> 洁净基线刻意把 Visit 清成 0，让走查人一眼看到目标单 —— 所以必须先派一次工。
> 这不是重复劳动，是**夹具要求**；但它的判定沿用第二轮结论，**只在它出问题时才红灯**。

> ⚠️ **不要**要求真人在同一张单上"门店自修 / 厂家各试一次"：
> 第一次派工成功后工单已 `PROCESSING`，再执行一次普通 `dispatch` **本就该被状态机拒绝**。
> 其余合法枚举由自动化契约断言覆盖（`smoke-test.mjs` §4d / `expected-h6-contract.mjs`），
> **真人无需把 API 回归测试再做一遍**。

---

## 3. 锁定的 PASS 门槛

> 本轮复测后，**同时**满足以下条件即建议 Phase 4 正式 PASS、放行 Phase 5。
> 「来源」列说明该项是**本轮复测**还是**沿用前轮结论**（沿用项若本轮观察到问题，立即降级为本轮必查）。

| # | 条件 | 来源 | 判定 |
|---|---|---|---|
| 1 | 改派**能提交成功**，且**原因不丢**（不再 `MISSING_REASON`） | **本轮复测（P0）** | ⬜ |
| 2 | 历史 Visit **不被覆盖**（Visit #1 仍在且为「已作废」） | **本轮复测（P0）** | ⬜ |
| 3 | 预约**只到天**，且界面**不出现**那个内部固定时刻；改约**不增加 Visit** | **本轮复测（P1）** | ⬜ |
| 4 | 详情能让人**自己回答**"当前谁处理 / 哪天上门 / 之前发生了什么" | **本轮复测（P1）** | ⬜ |
| 5 | 操作完成后的**状态 / Visit / 事件与服务端数据一致** | **本轮复测** | ⬜ |
| 6 | 真人**能够自行找到**「受理」并完成首次派工 | 沿用（第二轮已证） | ⬜ |
| 7 | **没有**误导性「编辑/删除」等按钮造成实际操作障碍 | 沿用（第二轮已证：未误导） | ⬜ |
| 8 | HQ 可查看完整结果，**另一门店仍隔离** | 沿用（首轮已证） | ⬜ |
| 9 | **没有出现必须由开发人员解释才能继续的阻塞性交互问题** | **本轮复测** | ⬜ |

### 不阻塞 Phase 4 的（登记 UX backlog 即可）

- 中文标签不够顺口、列宽 / 布局不好看
- 希望以后增加「今日待办」这类能力（→ Phase 9）
- 字段名需要解释但不妨碍完成工作
- 详情里"某条历史 Visit 为什么作废"需要点开才看清

> **判定原则**：只要**不妨碍完成工作**，就登记 backlog，**不阻塞 Phase 4**。

---

## 4. 主持规范

> ### ⚠️ 禁止预提示
>
> 本轮**仍然**不告诉走查人"该点哪个按钮"、"哪个字段什么意思"。
> 你只负责**说清任务**（§2 的"怎么做"列），不负责说**操作方式**。
>
> · 不指路、不说"那个按钮别点"、不解释字段含义；
> · 他点错、卡住、看不懂 —— 都是**要走查的结果**，不是需要立刻纠正的错误；
> · 只有在**彻底卡死无法继续**时才介入，并**如实记录"卡在哪、提示了什么"**。
>
> ⚠️ **不要**告诉走查人"上次那个原因的问题已经修好了" ——
> 那会让他**预期**这次一定成功，从而在失败时反复重试而不如实报告。
> 也不要问"是不是比上次好？" —— 让他自己说（§5）。

---

## 5. 本轮三个必答问题（**看详情之后立刻问，逐字记录**）

> 这是本轮 P1（信息层级）**唯一的**验收方式：不看他说"看得懂"，
> 只看他**能不能自己说出这三件事**。

| # | 问题 | 他的回答（逐字） | 判定 |
|---|---|---|---|
| ① | **"这张单现在是谁在处理？"** | ____________________________________________ | ⬜ 答对 / ⬜ 含糊 / ⬜ 答不出 |
| ② | **"预计哪一天上门？"** | ____________________________________________ | ⬜ 答对 / ⬜ 含糊 / ⬜ 答不出 |
| ③ | **"之前发生过什么？"**（能不能按顺序说出受理 → 派工 → 改派 → 改约） | ____________________________________________ | ⬜ 能 / ⬜ 漏项：__________ / ⬜ 说不出 |

**补充观察（不阻塞，用于排 backlog）**

| 观察点 | 记录 |
|---|---|
| 详情里是否**看得到内部编号 / 英文枚举 / 看不明白的字段**？ | ⬜ 没有 / ⬜ 有：__________ |
| 「已作废（SUPERSEDED）」这类说法他是否直接看得懂？ | ⬜ 能 / ⬜ 需解释 |
| 四块内容他是否**都找到了**（不必引导）？ | ⬜ 都找到 / ⬜ 漏了：__________ |
| 派工成功但客户可能**没收到短信**，他能否从界面察觉？ | ⬜ 能 / ⬜ 不能（→ Phase 9 决定是否需要"通知异常"标记） |
| 是否提出"我就想看今天该我处理的"这类诉求？ | ⬜ 有（记 Phase 9 工作台/SLA 输入） / ⬜ 无 |

---

## 6. 收尾提问（**比"在指导下点成功"更能说明可用性**）

全部操作完成后，主持人问**这一个问题**，并**逐字记录回答**：

> ### "如果明天再来一张工单，你知道应该从哪里开始处理吗？"

| 记录 | |
|---|---|
| 真人能否**说出**起点（例如"进'我的门店工单'→ 找'待受理'→ 点受理"） | ⬜ 能 / ⬜ 不能 |
| 逐字回答 | ____________________________________________ |
| 他是否**自己提到了**刚才用过的关键动作（受理 / 派工 / 改派 / 详情） | ⬜ 是 / ⬜ 否 |

> **判定口径**：能自己说清起点，说明这套后台**形成了可用心智模型**；
> 若回答不上来、只能说"点你刚才说的那个" ——
> 即使每一步都在指导下点成功，也**不能算"可用"**，据实记录。

---

## 7. 走查后数据复核（由验收人执行，**不依赖界面**）

| 项 | 走查前 | 走查后 | 预期 |
|---|---|---|---|
| 工单状态 | `NEW` | ________ | `PROCESSING` |
| Visit 条数 | 0 | ________ | **2**（#1 `SUPERSEDED` + #2 `ASSIGNED`） |
| Visit #2 的 `expected_visit_at` | — | ________ | 等于第 2 步改的**那一天** 的 `12:00（+08:00）`（内部固定时刻，界面不显示） |
| 改派原因是否落库 | — | ________ | 事件 `metadata.reason` 与 `summary` 都含真人填的原因 |
| 事件条数 | 0 | ________ | ≥ 4（accepted / dispatched / reassigned / rescheduled） |
| 事件顺序 | — | ________ | 受理 → 派工 → 改派 → 改约（`created_at` 升序） |
| 短信条数 | 0 | ________ | ≥ 3（派工双短信 + 改派 + 改约；`accepted` ≠ `delivered`） |
| 界面所见 vs 服务端 | — | ________ | **一致**（PASS 门槛第 5 条） |

```bash
# 复核命令（只读）
docker exec svc-postgres psql -U svc_app -d service_ticket -c \
  "select status, service_mode, provider_name, expected_visit_at from service_tickets where ticket_no='FW20260922-0059';"
docker exec svc-postgres psql -U svc_app -d service_ticket -c \
  "select id, visit_no, visit_status, superseded_reason, technician_mobile, \
          to_char(expected_visit_at AT TIME ZONE 'Asia/Shanghai','YYYY-MM-DD HH24:MI') as 预计上门 \
     from service_visits where ticket_id=1039 order by visit_no;"
docker exec svc-postgres psql -U svc_app -d service_ticket -c \
  "select event_type, from_status, to_status, summary, created_at \
     from ticket_events where ticket_id=1039 order by id;"
docker exec svc-postgres psql -U svc_app -d service_ticket -c \
  "select scene, status, error_code from sms_logs where ticket_id=1039 order by id;"
```

> ⚠️ 判断 Visit 状态看 **`visit_status`** 列（Phase 4-A 起它是生命周期的唯一事实来源；
> 老的 `status` 已降级为派生字段，拿它去判会得到空值 —— 见 DEV-73）。

---

## 8. 问题登记与结论

### 8.1 问题清单

| # | 步骤/观察项 | 现象 | 判定 | 整改 | 状态 |
|---|---|---|---|---|---|
| 1 | | | ⬜ 阻塞 Phase 4 / ⬜ 非阻塞 / ⬜ 记 Phase 9 | | ⬜ 未开始 |
| 2 | | | | | |

**判定说明**
- **阻塞 Phase 4**：误导性 UI、越权、操作被卡住（无法完成正常流程）；
  必须在关闭 Phase 4 前修掉，**修完需重跑被卡住的那一步**（不必重跑全部）。
- **非阻塞 / UX backlog**：文案、顺序、列宽、可优化的交互；登记后排期。
- **记 Phase 9**：超出本期范围的能力诉求（工作台、SLA、看板等）。

### 8.2 UX backlog（不阻塞 Phase 4）

| # | 内容 | 来源 | 建议排期 |
|---|---|---|---|
| 1 | 页面自动生成的「编辑 / 删除」按钮（服务端必拦，但看起来像可用入口） | 首轮 + 第二轮 | Phase 5 前如再被误导则升级为阻塞 |
| 2 | | | |

### 8.3 结论

| 项 | 结论 |
|---|---|
| §3 的 9 条 PASS 门槛是否**全部**满足 | ⬜ 是 / ⬜ 否（见 §8.1） |
| P0（改派原因丢失）是否确认修复 | ⬜ 是 / ⬜ 否 |
| P1（预计上门日期）是否确认整改且业务含义清楚 | ⬜ 是 / ⬜ 否 |
| P1（详情信息层级）三个必答问题是否都能答出 | ⬜ 是 / ⬜ 否 |
| 是否存在未关闭的误导性 UI 问题 | ⬜ 无 / ⬜ 有 |
| 是否存在未关闭的越权问题 | ⬜ 无 / ⬜ 有 |
| 是否存在未关闭的操作阻塞问题 | ⬜ 无 / ⬜ 有 |
| §6 收尾提问能否自己说清起点 | ⬜ 能 / ⬜ 不能 |
| **Phase 4 是否可以整体签 PASS** | ⬜ 可以 / ⬜ 不可以 |

**签署**

- 走查人（门店售后人员 UAT-A）：______________　日期：__________
- 组织人 / 验收人：______________　日期：__________
- 复核方：______________　日期：__________

---

## 9. 走查结束后的清理（**必做**）

> ⚠️ **走查后默认执行 `--disable`，不要执行 `--delete`**（首轮已裁定）。

**为什么不是直接物理删除**：走完之后 UAT-A 通常已是主工单的 `handler_user_id`，
受理 / 派工 / 改派 / 改约每条 `ticket_events` 的 `operator_user_id` 也指向它。
而 UAT 工单是**要保留的验收证据**。此时物理删除用户行，会让工单与事件的引用变成空引用 ——
证据"还在"，但**说不清是谁做的**，审计上不可解释。

本项 NocoBase 的 `users` 表**没有**可用的禁用字段，因此按降级口径执行：

```bash
# ① 【默认】停用：撤角色 + 撤门店映射 + 重置随机口令 + 阻断登录，**保留 users 行**
node scripts/uat-accounts.mjs --disable

# ② .env 里残留的 UAT_*_PASSWORD 已失效，可手工删除
#    （.env 不入库，删不删都不影响功能）

# ③ UAT 走查工单（FW20260922-0059 / 0060）**保留** —— 它们是走查证据。
#    若要清理，先确认已截图归档。
```

| `--disable` 动作 | 效果 |
|---|---|
| `DELETE FROM store_users` | 撤掉临时门店映射 → **数据范围归零** |
| `DELETE FROM "rolesUsers"` | 撤销业务角色 → 无任何业务权限 |
| `password` 重置为随机哈希 | 旧口令失效，不可知 |
| `username` / `email` / `nickname` 改为 revoked 形态 | **阻断登录入口** |
| **保留 `users` 行** | 工单 handler 与事件 operator 的历史引用**继续可解释** |

---

## 附 A：第二轮的 8 步执行记录（**历史留档，本轮不重跑**）

第二轮按"完整操作闭环"走，产出的判定就是 §0.2 那三项。留档的目的：
**下次有人问"为什么要改这三处"时，看这一节就够了。**

| # | 步骤 | 第二轮结果 | 本轮处置 |
|---|---|---|---|
| 1 | UAT-A 登录 → 找到主走查单 | ✅ 通过 | 沿用 |
| 2 | 点「受理」 | ✅ 通过（状态立即变「处理中」，**无需**额外成功提示） | 沿用 |
| 3 | 派工：厂家 / `UAT测试厂家` / 王师傅 | ✅ 通过（Visit #1） | 本轮顺带重做（夹具要求） |
| 4 | **改派李师傅 + 填原因** | ❌ **失败** → 服务端 `MISSING_REASON` | **P0，本轮必复测** |
| 5 | 对 Visit #2 改约 | 🟡 通过但**交互不合理**（要求选到分钟） | **P1，本轮必复测** |
| 6 | 打开详情逐块阅读 | 🟡 可达但**回答不了三个问题** | **P1，本轮必复测** |
| 7 | 切 UAT-HQ 看同一张单 | ✅ 通过 | 沿用 |
| 8 | 切 UAT-B（门店 B） | ✅ 通过（看不到、直链 404） | 本轮 §2 第 4 步顺带反证 |

---

## 附 B：开场台词（**不许多说**）

> "请在列表里找到 `FW20260922-0059` 这张报修单，
> **先把它受理，派给王师傅**（服务方式选厂家，厂家名称填 `UAT测试厂家`）；
> **然后改派给李师傅，原因随便写一句**；
> **再把预计上门日期改到后天**；
> **最后打开这张单的详情看一眼**。"

> ⚠️ 必须报出工单号（同店还有别的 `NEW` 单，不点名可能处理错单）。
> **报工单号只是指定对象，不是提示操作方式** —— "点哪里"仍然不能说。
>
> 说完**闭嘴看着他操作**。他找不到按钮、点错地方、看不懂文案 —— 这些都是要走查的结果，
> 不是需要你立刻纠正的错误。开发人员站在旁边说"点这里、再点那里"，
> 只能证明功能存在，证明不了后台能用。
