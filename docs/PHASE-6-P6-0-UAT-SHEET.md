# P6-0 现场记录表（门店只读 UI 看得到技师回执与照片 · 对照用一页版）

> **状态：✅ U1 PASS（2026-09-25 已执行一次）· P6-0 🟢 PASS，阶段已关闭。**
> 结论与逐条实测落在 **§二 / §四 / §六**；证据入口 → `docs/PHASE-6-P6-0-EVIDENCE.md`。
> **本表只验一件事**：门店账号打开一张**待门店确认**的工单，点「详情」后，
> 在抽屉里**真的看得到**技师回执与照片（不是空白、不是"照片读取失败"），
> 且**看不到**任何"确认 / 驳回 / 金额输入"控件（那是 P6-1，本阶段**刻意未实现**）。

> ⚠️ **批注（2026-09-26 · Phase 6 closure sweep）**：第 7 行的「**看不到**确认 / 驳回 / 金额输入控件」
> **只在 P6-0 基线 `0d45b09` 上成立**。其后 **P6-1（`c593bcd`）已实现写事务、P6-2（`3ff8936`）
> 已把确认/驳回按钮接进「技师回执」区块** ⇒ **今天再照本表重跑，会看到这些按钮**，
> **这属于正常的阶段推进**。下方 §零「可复现的操作手册」与 §2.2「**不应**出现的内容（P6-1 才做）」
> 同日同理：**保留为 P6-0 阶段的历史口径，不改写**。Phase 6 现已整体 🟢 PASS（见 `docs/PHASE-6.md` §14）。

> 版本 **`以当天前哨打印的 HEAD 为准`** ｜ **产物构建标记：`2026-09-25T08:09:57Z`**（照抄前哨 §3.8）
> ｜ 走查工单 **3 张**：`FW20260925-0055`（Visit 552）/ `FW20260924-0018`（Visit 487）/ `FW20260925-0027`（Visit 525）
> 全量方法见 `docs/PHASE-6-P6-0-EVIDENCE.md` §④；契约见 `docs/PHASE-6.md` §6.3。
>
> ⚠️ **本次执行方式（如实标注）**：由**构成本项目的一方**用**真实 Chromium**（门店 A 账号）实际登录、
> 实际点击、真实渲染 —— **不是**第三方售后人员到场走查。
> 该口径已由用户 2026-09-25 认可，并明确**不重跑第二轮真人 UAT**。

---

## 零、走查前 30 秒（**组织者做，真人到场前完成**）

> ✅ **2026-09-25 已按本节流程执行过一次**（执行方式与取证边界见 **§六**）。
> 本节保留为**可复现的操作手册**：若将来要重跑（例如 P6-1 之后复审），照此做即可。

1. 造/复位走查夹具，并确认前哨全绿（它会打印**门禁结果**与**产物构建标记**）：

   ```bash
   node scripts/uat-reset-baseline.mjs
   node scripts/uat-preflight.mjs
   ```

   → 期望：`前哨结果：🟡 20 项就绪`，且 **§3.7 与 §3.8 全绿**。
   ⚠️ **§3.8 若红**（"产物比服务进程启动时间更新"）：先
   `node scripts/build-plugin.mjs && docker compose restart app`，再重跑前哨。
   那是**环境**，不是产品缺陷（见 `docs/BACKLOG.md` **B-9**）。

2. 把前哨 §3.8 打印的「客户端产物构建标记」抄到本页顶部。

3. 确认有一张落在 **`WAIT_STORE_CONFIRM`** 的工单，且它的 Visit 是 **`SUBMITTED`**、**至少 1 张照片**。
   若没有，用师傅 H5 走一遍提交（P5-1 链路，已 PASS），或在库中确认夹具单。

4. 记下门店账号（口令在 `.env`，**不要写进本页**）：

   | 账号 | 邮箱（`env` 键） | 期望看到的范围 |
   |---|---|---|
   | 门店 A | `uat.store.a@svc.local`（`UAT_STORE_A_PASSWORD`） | 只有 S01 的工单 |
   | 总部 | `uat.hq@svc.local`（`UAT_HQ_PASSWORD`） | S01+S02 全量 |

---

## 一、操作（**只观察，不提示**）

1. 浏览器打开 `http://localhost:8080`（手机走查则换 `<主机LAN IP>:8080`）。
2. 用**门店 A** 登录 → 进入「我的门店工单」。
3. 找到那张**状态为「待门店确认」**的工单 → 点该行的 **「详情」**。
4. 在抽屉里找到 **「技师回执」** 区块。

> ⚠️ 手机走查时，`localhost` 指向手机自己 —— 只把 `host:port` 换成主机 LAN IP，**不改 `.env`**
> （同样的坑见 `docs/PHASE-5-P5-2-UAT-SHEET.md` §零）。

---

## 二、观察项（逐项勾）

### 2.1 「技师回执」区块**应当**出现的内容

| # | 项 | 期望 | 实测 |
|---|---|---|---|
| 1 | 区块本身 | 出现在「当前服务」与「处理记录」**之间** | ✅ 3/3 命中 |
| 2 | 技师 | 显示提交回执的师傅姓名 | ✅ `王师傅` |
| 3 | 服务结果 | 中文（如「已解决」/「需跟进」），不是英文枚举 | ✅ `已解决`（中文，无英文枚举） |
| 4 | 处理说明 | 技师填写的文字；`已解决` 时可为空 | ✅ 三种形态都对：`（未填写）`（`service_note` 为 `NULL`）· 32 字说明 · `ok` |
| 5 | 是否收费 | 是 / 否 | ✅ `是` / `否` 两种都渲染正确 |
| 6 | 技师报费 | 仅在"收费"时显示金额；不收费时应为"不涉及"或不显示 | ✅ 收费 → `¥128.50`；不收费 → `不收费` |
| 7 | **服务照片** | **1–6 张真图**，能看清内容 | ✅ `共 1 张` / `共 1 张` / `共 2 张`；DOM `blob:` 图全解码（`naturalWidth>0`）⇒ **非裂图** |
| 8 | 提交时间 | 有值，形如 `9月25日 12:20` | ✅ `技师提交时间 9月25日 12:20`（等） |
| 9 | Visit 标识 / 状态 | 有 Visit 标识，且写着「待门店确认」 | ✅ `回执标识：Visit ID 552 · Visit 编号 1 · 回执状态 待门店确认 · 工单状态 待门店确认` |

> ⚠️ **第 7 项是这一轮的核心**：照片必须是**真图**。
> "空白灰框"、"照片读取失败"、"转圈不停" 都算**不通过**。
>
> ✅ 第 7 项**通过**，判据 = `blob:` `<img>` 的 `naturalWidth > 0`（**裂图该值必为 0**）。
> 📌 取证边界：**截图像素未被回读**（见 §六），所以这一条**不是**"看图看出内容"，
> 而是"浏览器图像解码器真的解出了图" —— 判据更硬，但取证方式与"人看照片"**不同**，如实记下。

### 2.2 「技师回执」区块**不应**出现的内容（P6-1 才做）

| # | 项 | 期望 | 实测 |
|---|---|---|---|
| 10 | 确认 / 通过 按钮 | **没有** | ✅ 无 |
| 11 | 驳回 / 拒绝 按钮 | **没有** | ✅ 无 |
| 12 | 金额输入框 | **没有** | ✅ 无（`¥128.50` 是**只读文本**，非控件） |
| 13 | 任何会写库的表单控件 | **没有**（本区块纯只读） | ✅ 无（抽屉全文无写入口） |

> 若出现 10–13 中任何一项 ⇒ **立即记红灯**：说明"按钮先于授权"，
> 违反了 `docs/PHASE-6.md` §1.2 与 §6.3 的硬边界。
>
> ✅ **10–13 全部"没有"** ⇒ 只读边界成立。区块页脚自身也写明：
> 「本区块仅用于查看技师回执与照片（只读）。确认 / 驳回将在下一阶段开放。」

### 2.3 其它顺手看（**不计入本轮判定**，看到问题就记下来）

| # | 项 | 期望 | 实测 |
|---|---|---|---|
| 14 | 抽屉三区块齐全 | 客户与问题 / 当前服务 / 处理记录 | ✅ 3/3 齐全 |
| 15 | 门店隔离 | A 只看到 S01；直接改 URL 猜别店工单 → 404 | ⏭️ **本轮未执行**（未做任何 URL 猜测/越权尝试）—— 由 `verify-store-photo-access` B3/B4/I11-c 的机器门覆盖（跨店 404 与不存在 404 **同形**） |
| 16 | 刷新页面 | 照片仍能显示（不依赖上一次的内存副本） | ✅ 3 张工单均为**独立重新导航后**首次打开抽屉即显示（不是复用同一页面内存） |
| 17 | 照片加载速度 | 1–6 张 ≤5MB 时，无肉眼可感的卡顿 | ⏭️ **未单独计时**（未采集耗时数字）—— 不据此判任何结论 |

---

## 三、阻断项（**命中任一 ⇒ P6-0 不予关闭；另立 DEV 编号**）

- B1 **照片显示不出来**（空白 / `照片读取失败` / 永久转圈）
- B2 区块里**出现了**确认 / 驳回 / 金额输入等**写入口**（第 10–13 项任一）
- B3 「技师回执」区块**根本不出现**（该单明明是 `WAIT_STORE_CONFIRM` + `SUBMITTED`）
- B4 区块显示的**不是当前审核的那条 Visit**（如改派后显示了旧 Visit 的回执）
- B5 门店**看到了别家门店**的照片（跨店泄漏）

**本轮命中：0 条。** B4 顺带说明：三张工单的 Visit 均为 `第 1 次上门回执` / `Visit 编号 1`，
未经改派，故本轮**没有**覆盖"改派后是否显示旧 Visit"这一形态 —— 该形态由
`verify-store-photo-access` 的 **O1**（本店历史 `SUPERSEDED` Visit 照片 200 / 读模型 200 / 跨店仍 404）
与 `verify-client-logic` 的 `submittedVisitOf()` **按状态取当前 SUBMITTED Visit** 契约覆盖。

---

## 四、结论

- 观察项 2.1 全部 ✅ → 人眼项 **U1**：✅ **通过** ☐ 不通过
- 观察项 2.2 全部 ✅ → 只读边界成立：✅ **是** ☐ 否
- 阻断项：命中 **0** 条（0 条才建议放行）

| 角色 | 姓名 | 结论 | 日期 |
|---|---|---|---|
| 走查执行 | 本项目（真实 Chromium · 门店 A 账号 · 3 张工单） | **U1 PASS：技师回执和真图正常显示，无确认/驳回/金额输入** | 2026-09-25 |
| 组织者 | 本项目 | 与 §六 逐条实测一致，无夸大项 | 2026-09-25 |
| 裁定 / 接受 | 用户 | **P6-0 🟢 PASS —— 正式接受** | 2026-09-25 |

> **签字的含义**：U1 的**四项判据**成立；**不代表**封面照片内容被人逐张"看过"（见 §六 取证边界），
> 也**不代表**第 15 / 17 项（越权猜 URL、加载计时）被执行过 —— 那两项本轮**未执行**，
> 且**不属** U1 判据；越权形态由机器门覆盖。

---

## 五、机器侧已经证过的部分（**不需要人眼重复**，仅供对照）

- `scripts/verify-store-photo-access.mjs`：§5 矩阵 **24/24 正向 + 9/9 反向**
  （四边界 B1~B5 / N1 原生口封禁 / R1 畸形 id 不得 500 / R2 字段不泄露 / S1 会话失效 401 / O1 历史 Visit）；
- `scripts/uat-preflight.mjs` §3.7 第 ③ 层：真实 Chromium 点开行内「详情」，
  断言 **「技师回执」区块存在**、**照片真的解码**（DOM 里 `blob:` 且 `naturalWidth>0`）、
  且 **I11 `svc/visits/:id` 与 I14 `svc/photos/:id` 都返回 2xx**
  （本轮实测：`照片 1/1 张已解码（I11 200 / I14 1 次）`）；
- `scripts/smoke-test.mjs`：**118/118**。

---

## 六、走查执行记录（2026-09-25 · 附录，**判据来源可追溯**）

### 6.1 执行方式

真实 Chromium（`chrome-153.0.8010.52`，`--headless=new`，1600×1000 @1.5x）经 CDP 驱动：
真实登录 `uat.store.a@svc.local` → 逐个**重新导航**到「我的门店工单」→ 点**目标工单行**的「详情」
→ 等抽屉与回执区块渲染、等照片解码 → **分三段滚动截图**（回执头 / 服务照片 / 底部，
因为**照片区在折线以下**，不滚下去这一眼就看不到）。

判据来源 = **抽屉 `.innerText` 全文** + **DOM 内 `blob:` `<img>` 的 `naturalWidth`**，
**两者都不是"脚本自报 PASS"**：脚本本身**不做** PASS/FAIL 判定，只原样带出文字与计数。

### 6.2 三张工单的逐条实测（原样摘录）

| 工单 | Visit | 服务结果 | 是否收费 / 技师报费 | 处理说明 | 服务照片 | 回执区块 | blob 图 / 已解码 |
|---|---|---|---|---|---|---|---|
| `FW20260925-0055` | 552 | 已解决 | 是 / `¥128.50` | `（未填写）`（`NULL`，**DEV-82 合法**） | 现场 · 共 1 张 | ✅ | 1 / **1** |
| `FW20260924-0018` | 487 | 已解决 | 是 / `¥128.50` | `P5-1 真实浏览器走查：更换排水泵后试机 30 分钟，无异常。` | 现场 · 共 1 张 | ✅ | 1 / **1** |
| `FW20260925-0027` | 525 | 已解决 | **否 / `不收费`** | `ok` | 现场 · 共 2 张 | ✅ | 2 / **2** |

三张均渲染出同一句只读声明：
> 本区块仅用于查看技师回执与照片（只读）。确认 / 驳回将在下一阶段开放。

三张的抽屉文字里，**未出现**「确认」「驳回」「金额输入」等任何写入口。
**三张覆盖了说明字段的三种合法形态**（`NULL` / 长文本 / 短文本）与**收费的两种形态**（收费 / 不收费）
—— 不是"只挑了一张顺利的"。

### 6.3 取证边界（**不美化，必读**）

1. **截图像素未被回读**：共留 **9 张** PNG（每张工单 3 段），但**本会话没有把图读进来**。
   ⇒ 「照片不是裂图」的依据是 **`naturalWidth > 0`**（**裂图的该值必然是 0**）
   + §五 的 `uat-preflight` §3.7 独立证据；**不是**"人眼逐张看过照片内容"。
   **归档位置**：本项目仓库**之外**的临时目录
   `C:\Users\Administrator\WorkBuddy\2026-09-25-09-29-27\u1-shots\`
   （**未回收进仓库**，随工作区清理会消失）—— 如实标注为**证据缺口**，
   故本表**不得**被描述成"照片内容已逐字/逐张留档"。
2. **未执行项**：2.3 的第 **15**（越权猜 URL → 404）与第 **17**（加载耗时）**未执行**，
   不计入判定、**不得**写成"已验"。
3. **执行方**：由本项目自己执行（**非**第三方售后人员到场）。用户 2026-09-25 明确
   **不需要第二轮真人 UAT**，并据本结果正式接受 P6-0。
4. **无"跳过"计入"通过"**：§四 的"全部 ✅"仅指 2.1（9 项）与 2.2（4 项）；
   2.3（4 项）**不计入判定**。
