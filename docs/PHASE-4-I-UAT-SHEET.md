# Phase 4-I 现场记录表（**第四轮 · 只复测「详情」一条** · 打印/对照用）

> 版本 **`以当天前哨打印的 HEAD 为准`** ｜ **产物构建标记：`2026-09-23T07:18:45Z`**
> （写的是本轮的实测值；**每次重建产物都会变** —— 请以走查当天前哨 **§3.8** 打印的那一串为准）
> ｜ 目标单 `FW20260922-0059`（门店 S01）｜ 全量文档见 `docs/PHASE-4-I-UAT.md`

> **本轮只验一件事：打开「详情」抽屉。**
> 第三轮的结果是：**改派 ✅ / 改约 ✅ / 详情 ❌（HTTP 404）**。
> 前两项已 PASS，**本轮不再重测**；门店隔离、搜索、HQ 可见、编辑删除未误导等更早的结论也不再重验。
> 详情 404 的根因与修复见 `docs/DEVIATIONS.md` 的 **DEV-74**（两条：请求前缀 + 产物交付链）。

---

## 零、走查前 30 秒（**组织者做，真人到场前完成**）

1. 跑一次标准入口，确认前哨全绿：

   ```bash
   node scripts/uat-reset-baseline.mjs
   ```

2. 记下前哨 **§3.8** 打印的 **「客户端产物构建标记」**，填到本页顶部的下划线处。

> ⚠️ **为什么多了这一步**（DEV-74 的直接教训）：
> 上一轮自动化全绿、真人仍然 404。真因不是请求 URL（那个早已修好），
> 而是 **nginx 曾对插件产物发 7 天长缓存**，而产物 URL **不含内容哈希** ——
> 浏览器 7 天内根本不回源，于是**一直在跑几小时前的旧产物**。
> 探针每次都开全新 profile（空缓存）所以永远拿最新产物、永远绿；
> 只有真人会被粘住。**"自动化全绿"恰恰说明它测的不是真人的环境。**

3. 让真人**强制刷新一次**（Windows `Ctrl+Shift+R`），**并在 Console 里核对那行构建标记**。

---

## 一、开场（**只说这一句，然后闭嘴看着他操作**）

> **"请先按一下 `Ctrl+Shift+R`；然后在列表里找到 `FW20260922-0059` 这张报修单，
> 打开它的详情看一眼。"**

⚠️ **必须报出工单号**（同店当刻还有别的单，不点名他可能处理错对象；服务端复核按 `ticket_id=1039` 核对）。
报工单号**不是提示操作方式**（只是指定对象），可以说；**"点哪里"不能说**。

> **额外一句（本轮新增，必须说）**：
> **"如果打开后是空白或者提示加载失败，麻烦按 `F12`，把 Console 里那条
> `[service-ticket] 客户端产物构建 …` 的时间，和 Network 里那个红色请求的地址念给我。"**
>
> 这不是提示操作，而是**预置取证据的方式** —— 上一轮为了拿到"那一行 Request URL"，
> 花了整轮时间在猜。现在错误提示里已经直接带地址，Console 里也有版本标记。

🚫 **禁止**：指路 / 说"那个按钮别点" / 解释字段含义 / 替他点。
✅ **只在彻底卡死无法继续时**介入，并如实记下"卡在哪、提示了什么"。

> ⚠️ **不要**说"详情上次坏了、这次修好了"；也不要问"是不是比上次好？"。
> 让他自己说 —— 那才是验收方式。

---

## 二、前置核对（**两项都 ✅ 才继续**）

| # | 检查 | 怎么查 | 结果 |
|---|---|---|---|
| A | **浏览器跑的是最新产物** | Console 里 `[service-ticket] 客户端产物构建 …` 与本页顶部抄的标记**一致** | ⬜ 一致 / ⬜ 不一致：__________ / ⬜ 没这行 |
| B | 前哨 §3.6 / §3.7 / §3.8 无红灯 | 标准入口输出里三段都是 ✅ | ⬜ 是 / ⬜ 否（红灯内容：__________） |

> A 不一致（或压根没这行）⇒ **别测了，先查缓存/产物**：这不是产品缺陷，测出来的 404 不具证据力。

---

## 三、记录

### 3.1 前置操作（**不作为判定项，只为把单子做到"有历史可看"**）

> **为什么要做**：标准入口会把 `0059` 复位成 `NEW / 0 条 Visit / 0 条事件`。
> 此时抽屉里只有「尚未派工（等待门店受理）」和「暂无处理记录」，
> **三个问题根本无从回答**。所以先把单子做到有历史的状态。
>
> **为什么不算判定**：这四步在第三轮已经全部 PASS，本轮不重复判定；
> 只要不出现新的阻塞（崩、报错、卡死），就不记红灯 —— 但**出现了就要记**。

| # | 动作 | 只要确认 | 结果 |
|---|---|---|---|
| P1 | 受理 `FW20260922-0059` | 不报错，状态离开「待受理」 | ⬜ |
| P2 | 派给王师傅（服务方式选**门店自修**） | 不报错，出现第 1 次派工 | ⬜ |
| P3 | 改派给李师傅，原因随便写一句 | 不报错（第三轮已 PASS） | ⬜ |
| P4 | 把预计上门日期改到**后天** | 不报错（第三轮已 PASS） | ⬜ |

### 3.2 本轮**唯一**判定项

| # | 动作 | 预期 | 结果 | 备注 |
|---|---|---|---|---|
| 1 | **打开 `FW20260922-0059` 的详情** | 抽屉打开，能**自己说清**三个问题（见第四节） | ⬜ | 若失败：错误提示里出现的地址：____________ |

> 备选（若不想做前置操作）：库里 `FW20260922-0060`（门店 S02）**已经带着**
> 派工 + 改派（王师傅 → 李师傅，原因「阿萨德」）的历史与「预计上门」日期。
> 用它测详情必须改用 **UAT-B** 账号登录，其余步骤不变（但时间线里**不会出现受理与改约**）。

---

## 四、本轮核心：三个必答问题（**看完详情立刻问，逐字记录**）

| # | 问题 | 他的回答（逐字） | 判定 |
|---|---|---|---|
| ① | **这张单现在是谁在处理？** | ________________________ | ⬜ 答对 / ⬜ 含糊 / ⬜ 答不出 |
| ② | **预计哪一天上门？** | ________________________ | ⬜ 答对 / ⬜ 含糊 / ⬜ 答不出 |
| ③ | **之前发生过什么？**（能否按序说出 受理→派工→改派→改约） | ________________________ | ⬜ 能 / ⬜ 漏项：____ / ⬜ 说不出 |

**顺手看一眼（不阻塞，用于排 backlog）**

| 观察点 | 记录 |
|---|---|
| 抽屉打开**快不快**（是否长时间转圈） | ⬜ 快 / ⬜ 慢：约 ____ 秒 |
| 详情里出现**内部编号 / 英文枚举 / 看不懂的字段**？ | ⬜ 没有 / ⬜ 有：__________ |
| 「已作废」这类说法直接看得懂？ | ⬜ 能 / ⬜ 需解释 |
| 同一件事**说了两遍**了？（「处理记录」应只有一条线） | ⬜ 没有 / ⬜ 有：__________ |

---

## 五、PASS 门槛（**全部满足**才建议放行 Phase 5）

**本轮复测的（1 条，是这次来的目的）**

1. ⬜ 详情能让人**自己回答**"当前谁处理 / 哪天上门 / 之前发生了什么"（且前置核对 A、B 均为 ✅）
   —— 具体证伪条件：**抽屉打不开、或出现「加载失败」、或三问里任一答不出/含糊，即不满足**

**沿用前三轮结论的（其余 8 条，本轮若观察到问题立即升级为本轮必查）**

2. ⬜ 改派**能提交成功**，且**原因不丢**（第三轮已证）
3. ⬜ 历史 Visit **不被覆盖**（Visit #1 仍在且「已作废」）（第三轮已证）
4. ⬜ 预约**只到天**、界面**不出现**内部固定时刻；改约**不增加 Visit**（第三轮已证）
5. ⬜ 状态 / Visit / 事件**与服务端数据一致**
6. ⬜ 真人能**自行找到**「受理」（第一轮已证）
7. ⬜ **无误导性「编辑/删除」**造成操作障碍（第一轮已证：未误导 → 记 UX backlog）
8. ⬜ HQ 可见完整结果，**另一门店仍隔离**（第一轮已证）
9. ⬜ **无必须由开发人员解释才能继续的阻塞性问题**

**不阻塞的**（登记 UX backlog 即可）：中文标签不顺口 / 列宽不好看 /
"以后能加今日待办吗" / 字段名需解释但不妨碍干活。
另记 UX backlog：列表里「查看 / 编辑 / 删除」与自研「详情」并存，
对一线是"我到底该点哪个"的认知负担（**与安全无关**，ACL 已保护）——
待 404 关闭后再收敛为「详情｜受理｜派工｜改派｜改约」按状态动态显示。

---

## 六、操作后服务端复核（不依赖界面）

```bash
docker exec svc-postgres psql -U svc_app -d service_ticket -c \
  "select status, service_mode, provider_name, expected_visit_at from service_tickets where ticket_no='FW20260922-0059';"
docker exec svc-postgres psql -U svc_app -d service_ticket -c \
  "select visit_no, visit_status, superseded_reason, \
          to_char(expected_visit_at AT TIME ZONE 'Asia/Shanghai','YYYY-MM-DD HH24:MI') as 预计上门 \
     from service_visits where ticket_id=1039 order by visit_no;"
docker exec svc-postgres psql -U svc_app -d service_ticket -c \
  "select event_type, from_status, to_status, summary from ticket_events where ticket_id=1039 order by id;"
```

| 项 | 走查前（标准入口刚复位） | 走查后 | 预期 |
|---|---|---|---|
| 工单状态 | `NEW` | ______ | `PROCESSING`（前置操作造成，属正常） |
| Visit 条数 | 0 | ______ | **2**（#1 `SUPERSEDED` + #2 `ASSIGNED`） |
| 事件条数 | 0 | ______ | **≥ 4**（派工 / 改派 / 改约 等业务事件，短信结果通知另计） |
| ⚠️ 打开详情**本身** | — | — | **不产生任何写操作、不增加事件**（若条数在"只点详情"后又变了，说明有副作用） |

> ⚠️ Visit 状态看 **`visit_status`** 列（老的 `status` 已降级为派生字段，拿它判会得到空值）。
> ⚠️ 判定项（打开详情）**是只读的**：只点详情不应当改变任何服务端数据。
> 若"只点详情"之后条数又变了，说明打开抽屉有副作用 —— 那是必须当场查的问题。

---

## 七、自动化侧的对应证据（**机器能证明的那一半**）

| 断言 | 脚本 / 段落 | 它证明什么 |
|---|---|---|
| 真实点击**行内**「详情」→ 捕获实际 HTTP → `svc:timeline` 与 `svc:visits` **均 2xx** → 三区块渲染 | `scripts/uat-preflight.mjs` **§3.7** | "点开之后真的有数据"，且**地址是对的**（不再是"只回读文字"） |
| 服务端实际返回的产物 == 刚构建的产物；静态产物**不得长缓存** | `scripts/uat-preflight.mjs` **§3.8** | "浏览器拿得到的是这一版"（DEV-74 的交付链） |
| 把抽屉请求改回缺陷形态 ⇒ §3.7 **必须变红**并带出 404 | `scripts/verify-detail-gate-reverse.mjs` | 闸门**不是**假闸门（铁律 8） |

---

## 八、签字

- 走查人（门店售后人员 UAT-A）：____________　日期：__________
- 组织人 / 验收人：____________　日期：__________
- **Phase 4 可否签 PASS**：⬜ 可以 / ⬜ 不可以

> 走查结束后执行 `node scripts/uat-accounts.mjs --disable`（**不要** `--delete`）；
> UAT 工单 `0059 / 0060` **保留**作为证据。
