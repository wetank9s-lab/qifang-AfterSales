# Phase 5-P5-2 — 手机真人走查（UAT）

> **状态：⬜ PENDING（待执行）**
>
> **被测基线：`297e728`**（P5-1 正式交付基线；**P5-2 不改代码**）
> ｜ 走查当天以前哨打印的 `git rev-parse --short HEAD` 与产物构建标记为准
> ｜ 打印 / 对照用一页版：`docs/PHASE-5-P5-2-UAT-SHEET.md`
>
> ⚠️ **本文件是公开仓库的一部分。** 走查人只记**代号**（`技师 UAT-T1`），
> **不写真实姓名、手机号、微信、身份证**；**不写任何口令**；工单号可写（`FW...` 是系统流水号，不含客户个人信息）。

---

## 0. 这一轮要证的是什么（**先读这一节，避免重复劳动**）

### 0.1 已由**机器**证明、本轮**不重验**的结论

> 来源：`docs/PHASE-5-P5-1-EVIDENCE.md`（P5-1 四组证据，基线 `297e728`）。

| 已证结论 | 来源 | 处置 |
|---|---|---|
| 短链 `/t/{token}` → **302** → `/h5/technician/visit/{token}`，且 `Location` 是**相对路径** | 证据 ④ ① ／ `verify-technician-routing` | ✅ 保留，**不重验**（除非手机端打不开，见 §6 阻断项） |
| 全部失效 Token 对外统一 **`401 TOKEN_INVALID`**，四种失效响应体**逐字节相同（68B）** | 证据 ① | ✅ 保留，不重验 |
| 照片 **magic bytes 判定** / EXIF+GPS 剥离 / 私有目录不可直读 / 属主绑定 | 证据 ② | ✅ 保留，不重验 |
| Submit **单事务原子性**（Visit→SUBMITTED / Ticket→WAIT_STORE_CONFIRM / Token 用后即焚 / 写事件） | 证据 ③ | ✅ 保留，不重验 |
| **0 张照片被服务端权威拒绝**（`422 PHOTO_REQUIRED`），"上传 1 张→失效到 0 张→仍拒" | 证据 ⑥b ／ submit 矩阵 P1~P4 | ✅ 保留，不重验 |
| 真实 Chromium 6 步闭环（含 DEV-80 收费分支）**全绿** | 证据 ④ | ✅ 保留，**但仍是"自动化视角"**，见 §0.3 |

### 0.2 本轮要证伪的命题

> **一个没参与开发、没看过页面的普通技师，拿着短信里那样一条链接，在真实手机上，**
> **能不能在不被告知"点哪里"的前提下，独立完成一次上门回执，**
> **并且看完结果后能用他自己的话分清：这是"已提交、等门店确认"，不是"工单已办结"。**

一句话：**机器已证明系统正确；P5-2 验的是"人能不能正确理解系统"。**

### 0.3 为什么这一轮**不能**由自动化替代

| 维度 | 自动化（CDP 真实浏览器）已经证明 | 自动化**证明不了** |
|---|---|---|
| 入口 | 探针能打开链接、step① 302 成功 | **真实手机**（微信内置浏览器 / 系统浏览器 / 不同分辨率）能否打开 |
| 操作 | 脚本能按选择器点中按钮、传文件 | 人**看不看得懂**要给哪张照片、按钮在不在拇指可及处 |
| 理解 | 断言页面**含**「已提交，等待门店确认」 | 人**读完之后是否真这么认为**——会不会理解成"已经结单了" |
| 语义 | 服务端回了 `WAIT_STORE_CONFIRM` | 人是否把"等门店确认"读成了"等我自己再处理" |

> ⚠️ **探针永远开全新 profile、永远拿最新产物、永远知道该点哪** ——
> 它测的不是真人的环境，也不是真人的心智。这正是 P5-1 期间 DEV-80 的教训：
> **API 层全绿 ≠ 页面走得通**；P5-2 再往前一步：**页面点得通 ≠ 人看得懂**。

---

## 1. 走查前置条件（执行前逐条勾掉）

| 项 | 要求 | 状态 |
|---|---|---|
| 代码版本 | 工作区干净；`git rev-parse --short HEAD` = **`297e728`**（或现场打印值） | ⬜ 现场确认 |
| 环境 | `docker compose ps` 三容器 healthy；`curl -s localhost:8080/api/svc/health` 返回 `status: ok` | ⬜ 现场确认 |
| 前哨 | `node scripts/uat-preflight.mjs` 全绿（含 §3.8 产物交付链、§3.9 师傅路由） | ⬜ 现场确认 |
| **手机可达** | **手机与主机同局域网**，手机浏览器能打开 `http://<主机LAN IP>:8080/api/svc/health`（见下「手机可达性」） | ⬜ 现场确认 |
| 走查夹具 | `node scripts/walkthrough-p5-1.mjs setup` 打印出**真实短信形状的短链** | ⬜ 现场确认 |
| 夹具复位 | `node scripts/uat-reset-baseline.mjs` 全绿（先跑，再填本表） | ⬜ |
| 走查人 | **未参与本项目的普通技师**（不让他看过任何页面 / 截图 / 文档） | ⬜ |
| 主持方式 | **不给操作指引**（见 §4「禁止预提示」） | ⬜ |
| 手机 | 机型 / 系统 / 浏览器与版本：____________________ | ⬜ |

### 手机可达性（**这是"手机端打不开"的前提，必须与产品缺陷区分开**）

本地 `PUBLIC_BASE_URL=http://localhost:8080` 里的 `localhost` **在手机上指向手机自己**，
因此**不能把 `node scripts/walkthrough-p5-1.mjs setup` 打印的原样短链拿去手机点**。
走查当天按下述步骤换成**手机可达的基址**（**只换 host:port，不动 token，也不改 `.env` 语义**）：

```bash
# ① 取主机 LAN IP（挑那个真实网卡地址，通常形如 192.168.x.x；
#    172.30.* / 192.168.190.* / 192.168.110.* 之类多为 Docker/WSL 虚拟网卡，勿用）
ipconfig            # 或 PowerShell: Get-NetIPAddress -AddressFamily IPv4

# ② 确认 8080 已发布到 0.0.0.0 且手机可打开
#    手机浏览器访问： http://<主机LAN IP>:8080/api/svc/health   → 期望 {"db":"ok","sms":"mock","tasks":"ok"}

# ③ 把 setup 打印的短链换成 LAN 基址（token 不变）：
#    http://localhost:8080/t/<token>   ⇒   http://<主机LAN IP>:8080/t/<token>
```

> ⚠️ **Windows 防火墙**：若手机打不开，先查是否为**入站规则拦了 8080**（属**环境问题，不是产品缺陷**）。
> ⚠️ **不要**为了方便把 `PUBLIC_BASE_URL` 永久改成 LAN IP —— 那是本机自测口径，
> 改动会污染 §9 闸门 3 的判据。走查用**临时替换链接**即可。

### 环境事实（由前哨固定，走查前填写一次）

| 项 | 值 |
|---|---|
| **走查执行版本**（走查当天 HEAD） | `________` ← 前哨现场打印 `git rev-parse --short HEAD` |
| **功能代码基线** | **`297e728`**（P5-1 交付基线） |
| **客户端产物构建标记** | `________` ← 前哨 §3.8 打印值（每次重建都会变） |
| 主机 LAN 基址（手机可达） | `http://__________:8080` |
| 日期时间 | ________________（开始）／________________（结束） |
| 走查人 | 普通技师 **UAT-T1**（**未参与开发、未见过页面**） |
| 主走查工单 | `FW__________`（id=____，Visit id=____）← `setup` 打印值 |
| 起始基线 | Visit `ASSIGNED` / Ticket `PROCESSING` / 事件 ≥3 条 / 照片 0 张 |

### 走查前标准准备（**顺序不可反**）

```bash
# ① 全量门禁 + 复位洁净基线（前哨会打印 HEAD 与产物标记）
node scripts/uat-reset-baseline.mjs

# ② 造走查夹具：真单 + 真派工 + 从 mock 发件箱取**真实短信里的那枚 Token**
node scripts/walkthrough-p5-1.mjs setup
#    → 打印：工单号 / Visit id / Token / 短信 seq / 短链 / 302 落点
```

> 顺序理由同 Phase 4：`smoke-test` 会**真实调用匿名报修接口造工单**，
> "先复位再验证"等于白复位（DEV-61 / DEV-63）。

---

## 2. 本轮**唯一**任务：独立完成一次上门回执

> 走查人拿到的**只有一句任务**（见 §4 与附录 A），**没有**"点哪里"的提示。

| 环节 | 走查人要做的（**只说任务，不说操作方式**） | 主持人不做 |
|---|---|---|
| 入口 | 打开你收到的那条链接 | 不替他点、不告诉他链接"其实是 302" |
| 上传 | 传**现场照片**（不规定张数，看他自行理解） | 不说"最少 1 张、最多 6 张" |
| 回执 | 填处理结果；如**本次收费**则填金额 | 不说收费框是**条件渲染**才出现 |
| 提交 | 提交 | 不说按钮什么时候可点 |
| 收尾 | 提交后**读一遍页面告诉他你看到了什么** | 不解释"等待门店确认"是什么意思 |

> ⚠️ **不要**在走查人操作前演示一遍，也不要在他卡住时"顺手帮他点一下再说没事"。
> 卡住本身就是**要走查的结果**（见 §6 阻断项）。

---

## 3. 锁定的 PASS 门槛

> 走查后，**同时**满足以下条件即建议 **Phase 5 正式 PASS**、进入下一阶段。

| # | 条件 | 判定 |
|---|---|---|
| 1 | 手机**能**从短信形状的 `/t/{token}` 打开师傅页（**不是**"环境没通"，见 §1 手机可达性） | ⬜ |
| 2 | 技师**在无提示下**完成：上传 ≥1 张照片 → 填回执 → 提交成功 | ⬜ |
| 3 | 提交后页面**明确**显示"已提交、等待门店确认"类终态；技师**能正确复述**（§5 问①） | ⬜ |
| 4 | 收费分支（若本次收费）**能填、能提交**，无静默失败（DEV-80 在真实手机上的回归） | ⬜ |
| 5 | 技师能说清照片的**上传方式与张数上限**（§5 问③） | ⬜ |
| 6 | **未出现** §6 任何一个**阻断项** | ⬜ |
| 7 | 界面所见与服务端数据**一致**（§7 复核） | ⬜ |
| 8 | **没有**出现必须由开发人员解释才能继续的阻塞性交互问题 | ⬜ |

### 不阻塞 Phase 5 的（登记 `docs/BACKLOG.md` 即可）

- 纯**间距 / 字体 / 字号 / 按钮视觉 / 配色**问题
- 文案**不够顺口**、图标**不够直观**（不影响完成）
- 个别字段名**需要解释**，但不妨碍完成工作
- 想增加"拍照直接调起相机"这类**体验增强**诉求

> **判定原则**：只要**不妨碍完成工作、也不造成理解错误**，就登记 backlog，**不阻塞 Phase 5**，
> **更不重开 P5-1**（P5-1 已 PASS 并冻结在 `297e728`）。

---

## 4. 主持规范

> ### ⚠️ 禁止预提示
>
> 你只负责**说清任务**（§2 / 附录 A），**不负责说操作方式**。
>
> · **不指路**、不说"那个按钮别点"、不解释字段含义、不替他点、不在旁边看着补一句；
> · 他点错、卡住、看不懂 —— 都是**要走查的结果**，不是需要立刻纠正的错误；
> · 只有在**彻底卡死无法继续**时才介入，并**如实记录"卡在哪、提示了什么"**。
>
> ### ⚠️ 禁止提前"剧透"结论
>
> **不要**说"这个我们已经测过了、没问题"；**不要**说"照片至少传一张"；
> **不要**问"是不是很好用？"—— 让他自己说（§5）。
>
> ### ⚠️ 不引导问法
>
> §5 的三个问题要**按原话问**，问完**闭嘴记录**。
> 不要在他答不出时给选项（"是不是意思是等门店确认？"）—— 那会把答案喂给他，验收就失效了。

---

## 5. 三个必答问题（**操作结束后立刻问，逐字记录**）

> 看**他自己怎么说**，不看他说"看得懂"。
> 三问分别对应三个最容易"机器对、人误解"的点。

| # | 问题（**原话**） | 他的回答（逐字） | 判定 |
|---|---|---|---|
| ① | **"你觉得刚才已经把整个工单办结了吗，还是还要门店处理？"** | ____________________________________ | ⬜ 答对（还要门店处理）／⬜ 理解成"已结单"（**阻断项**）／⬜ 含糊 |
| ② | **"如果需要收费，你知道金额填在哪里吗？"** | ____________________________________ | ⬜ 知道／⬜ 不知道（录 §6 观察）／⬜ 本轮无收费场景 |
| ③ | **"照片怎么上传、最多能传多少张，你能看明白吗？"** | ____________________________________ | ⬜ 能说清（含上限）／⬜ 说得出上传但**不知上限**／⬜ 说不出 |

**补充观察（不阻塞，用于排 backlog）**

| 观察点 | 记录 |
|---|---|
| 从点开链接到页面可用，**等了多久**？ | ⬜ 快 / ⬜ 慢：约 ____ 秒 |
| 首屏按钮 / 字号在**单手拇指**下是否够大、够到？ | ⬜ 都够 / ⬜ 有困难：__________ |
| 上传照片的入口**一眼可见**吗（没提示的前提下）？ | ⬜ 一眼可见 / ⬜ 找了 ____ 秒 |
| 提交按钮**什么时候变可点**，他是否理解（0 张时置灰是否会困惑）？ | ⬜ 理解 / ⬜ 困惑：__________ |
| 页面里有没有**看不懂的字段名 / 英文枚举 / 内部编号**？ | ⬜ 没有 / ⬜ 有：__________ |
| 他有没有提出**想直接调起相机拍照**之类的诉求？ | ⬜ 有（→ backlog） / ⬜ 无 |

---

## 6. 阻断项 vs 非阻断项（**这一节决定 Phase 5 能不能关**）

### 6.1 阻断项（命中**任一** ⇒ **影响 Phase 5 最终关闭**，需另立 DEV 编号修复）

| # | 阻断项 | 说明 |
|---|---|---|
| B1 | **不会上传** | 在无提示下**找不到或完不成**照片上传 |
| B2 | **不会提交** | 填完回执后**无法完成提交**（按钮不可达 / 点了没反应 / 一直失败） |
| B3 | **误以为已经结单** | 把"已提交待门店确认"读成"工单已办结"（§5 问①命中） |
| B4 | **收费分支无法完成** | 本次收费场景下金额框不出现 / 填了提交失败（DEV-80 在真机回归） |
| B5 | **手机端实际打不开** | 手机从 `/t/{token}` 打不开页面（**须先排除环境不可达**，见 §1） |

> ⚠️ **B5 特别说明**：先按 §1「手机可达性」确认是**环境**还是**产品**。
> 若手机连 `http://<LAN IP>:8080/api/svc/health` 都打不开 ⇒ **环境问题**，不记 B5；
> 若健康检查能开、但 `/t/{token}` 打不开 ⇒ **B5 成立**（产品问题）。

### 6.2 非阻断项（只进 `docs/BACKLOG.md`）

- 间距 / 字体 / 字号 / 配色 / 图标视觉
- 文案不顺口、措辞可优化
- 字段名需要解释
- "能不能调起相机""能不能传更多张"等**超出本期口径**的诉求

> ⚠️ **两类问题必须分清**：
> **B3（误以为已结单）是语义问题，属阻断**；**"终态文案字体不好看"是视觉问题，属 backlog**。
> 把语义问题当视觉问题放过，正是 P5-2 存在的意义。

---

## 7. 走查后数据复核（由验收人执行，**不依赖界面**）

```bash
# 复核命令（只读）
docker exec svc-postgres psql -U svc_app -d service_ticket -c \
  "select id, visit_no, visit_status, service_result, is_charged, reported_charge_amount, \
          (token_used_at is not null) as token_used, submitted_at \
     from service_visits where ticket_id=<ticket_id> order by visit_no;"
docker exec svc-postgres psql -U svc_app -d service_ticket -c \
  "select status, completed_at, reviewed_at from service_tickets where id=<ticket_id>;"
docker exec svc-postgres psql -U svc_app -d service_ticket -c \
  "select id, photo_type, mime, size, sort_order from service_visit_photos where visit_id=<visit_id> order by sort_order;"
docker exec svc-postgres psql -U svc_app -d service_ticket -c \
  "select event_type, from_status, to_status, summary, metadata from ticket_events where ticket_id=<ticket_id> order by id;"
```

| 项 | 走查前 | 走查后 | 预期 |
|---|---|---|---|
| Visit 状态 | `ASSIGNED` | ______ | **`SUBMITTED`** |
| Visit `token_used_at` | 空 | ______ | **非空**（用后即焚） |
| Ticket 状态 | `PROCESSING` | ______ | **`WAIT_STORE_CONFIRM`** |
| Ticket `completed_at` / `reviewed_at` | 空 | ______ | **仍为空**（**工单未完成**——直接印证 §5 问①） |
| 照片张数 | 0 | ______ | **≥ 1** |
| 收费口径 | — | ______ | 收费 → 金额 > 0；不收费 → 金额为 `NULL` |
| 事件条数 | ≥3 | ______ | **+1**（形如 `technician_submitted`） |
| 事件 metadata | — | ______ | 含 `photo_count`（= 实际张数） |
| **复核：再点一次链接** | — | ______ | **不能再进表单**（Token 已消费，`401 TOKEN_INVALID`） |
| 界面所见 vs 服务端 | — | ______ | **一致**（PASS 门槛第 7 条） |

> ⚠️ 判定 Visit 状态看 **`visit_status`** 列（Phase 4-A 起它是生命周期唯一事实来源；
> 老的 `status` 已降级为派生字段，拿它判会得到空值 —— 见 DEV-73）。
> ⚠️ **照片张数与事件 `metadata.photo_count` 必须相等** —— 不等说明有写入不一致。

---

## 8. 问题登记与结论

### 8.1 问题清单

| # | 步骤 / 观察项 | 现象 | 判定（阻断 Phase 5 / backlog） | 整改 | 状态 |
|---|---|---|---|---|---|
| 1 | | | ⬜ 阻断（B__） / ⬜ backlog | | ⬜ 未开始 |
| 2 | | | | | |

**判定说明**
- **阻断 Phase 5**：命中 §6.1 的 B1~B5 之一 ⇒ **另立 DEV 编号**，修完**需重跑被卡住的那一步**
  （不必重跑全部；但**重跑必须仍由真人、仍无提示**）。
- **backlog**：视觉 / 措辞 / 体验增强 ⇒ 登记 `docs/BACKLOG.md`，**不阻塞**。

### 8.2 结论

| 项 | 结论 |
|---|---|
| §3 的 8 条 PASS 门槛是否**全部**满足 | ⬜ 是 / ⬜ 否（见 §8.1） |
| §5 问①（是否分清"待门店确认"vs"已结单"） | ⬜ 答对 / ⬜ 误以为结单（**阻断**） |
| 是否出现 §6.1 任一**阻断项** | ⬜ 无 / ⬜ 有：__________ |
| 是否出现**必须开发人员解释**才能继续的问题 | ⬜ 无 / ⬜ 有 |
| **Phase 5 是否可以整体签 PASS** | ⬜ 可以 / ⬜ 不可以 |

**签署**

- 走查人（技师 UAT-T1）：______________　日期：__________
- 组织人 / 验收人：______________　日期：__________
- 复核方：______________　日期：__________

---

## 9. 走查结束后的清理（**必做**）

> ⚠️ 走查后会新建**走查账号**（若走查人需登录后台看结果）与**走查工单**；
> 账号默认执行 `--disable`，**不要** `--delete`（理由同 `docs/PHASE-4-I-UAT.md` §9：
> 走查工单的事件 `operator_user_id` 指向该账号，物理删除会让历史引用变成空引用）。

```bash
# ① 【默认】停用走查账号：撤角色 + 撤门店映射 + 重置随机口令 + 阻断登录，**保留 users 行**
node scripts/uat-accounts.mjs --disable
#   （P5-2 的技师是**匿名**，通常无需后台账号；仅当验收人另开账号时执行）

# ② 走查工单与 Visit/照片/事件 **保留** —— 它们是本轮的验收证据。
#    若要清理，先确认已按 §7 截图 / 导出归档。

# ③ 若走查中临时替换链接基址，确认 .env 的 PUBLIC_BASE_URL 未被改动：
grep -n "PUBLIC_BASE_URL" .env      # 期望仍是 http://localhost:8080
```

---

## 附 A：开场台词（**不许多说**）

> **"你手机上会收到（或我给你）一条链接，请打开它。**
> **就当这单今天你上门已经干完了 —— 该传的传上去，该填的填上，然后提交。**
> **弄完跟我说一声。"**

> ⚠️ **只说任务，不说操作方式**：不说"先传照片"、不说"选好处理结果"、不说"记得填金额"、
> 不说"按钮在右下角"、不说"至少传一张"。**"点哪里"不能说。**
>
> 说完 **闭嘴看着他操作**。他找不到按钮、点错地方、看不懂文案 —— 这些都是要走查的结果，
> 不是需要你立刻纠正的错误。开发人员站在旁边说"点这里、再点那里"，
> 只能证明功能存在，证明不了**师傅自己能用**。

---

## 附 B：自动化侧对应证据（**机器已经证明的那一半**）

> P5-2 不需要重跑这些；列在此处是为了让"人+机器"两半证据合在一处可复核。

| 断言 | 脚本 / 段落 | 它证明什么 |
|---|---|---|
| 短链 302 → H5；畸形短链 404；三路由 401 `TOKEN_INVALID`；失败体逐字节一致 | `scripts/verify-technician-routing.mjs`（11 项） | 入口与认证层在**真实 HTTP** 上成立 |
| Token 失效矩阵 8 格 + 反枚举 | `scripts/verify-technician-token-matrix.mjs`（12 项） | Token 生命周期语义成立 |
| 上传安全矩阵（magic bytes / EXIF / 私有 / 属主） | `scripts/verify-technician-upload.mjs`（20 项） | 照片链路安全口径成立 |
| Submit 原子性 + R1/R2 反向 + **照片 1–6 张下限（P1~P4）** | `scripts/verify-technician-submit.mjs`（**14 项**） | 提交事务边界与照片口径成立 |
| 师傅 H5 契约（含 fixture 自检 + 变异测试 7/7） | `verify-technician-h5*.mjs`（33 / 13 / 7） | 前端契约成立，且**checker 自身被验证器验过** |
| **真实 Chromium 6 步闭环**（含 DEV-80 收费分支） | `scripts/walkthrough-p5-1-browser.mjs` | 页面在**自动化视角**下走得通 |
| 产物交付链（产物不晚于服务进程启动） | `scripts/verify-bundle-delivery.mjs` | 手机**拿得到**这一版产物（DEV-74 的教训） |

> 📄 逐条实测值见 `docs/PHASE-5-P5-1-EVIDENCE.md`。
> **P5-2 要做的是补上这张表右列缺失的那一格：真人、真机、无提示、能正确理解。**
